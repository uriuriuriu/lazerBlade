export const pageTitle = ({ page }) => { return page.title }
export const loadingCount = ({ page }) => { return page.loadingCount }
export const loadingSets = ({ page }) => { return page.loadingSets }
